<section class="spotlight">
    <h2>Media Spotlight:</h2>
    <h2 class="featured-work"><%= link_to @featured.title, work_path(@featured.id) %> by <%= @featured.creator %></h2>
</section>

<section class="top-ten-trio">
    <section class="top-ten-section">
        <h2>Top Movies</h2>
        <ul class="top-ten-list">
            <% @movies.each do |movie| %>
            <li class="list-group-item">
                <h4><<%= movie.title %></h4> by <small><%= movie.creator %></small>
            </li>
            <% end %>
        </ul>
    </section>

    <section class="top-ten-section">
        <h2>Top Books</h2>
        <ul class="top-ten-list">
            <% @books.each do |book| %>
            <li class="list-group-item">
                <h4><%= book.title %></h4> by <small><%= book.creator %></small>
            </li>
            <% end %>
        </ul>
    </section>

    <section class="top-ten-section">
        <h2>Top Albums</h2>
        <ul class="top-ten-list">
            <% @albums.each do |album| %>
            <li class="list-group-item">
                <h4><%= album.title %></h4><small> by <%= album.creator%></small>
            </li>
            <% end %>
        </ul>
    </section>


</section>